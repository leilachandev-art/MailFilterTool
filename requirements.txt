Flask==3.0.3
Flask-SQLAlchemy==3.1.1
Flask-WTF==1.2.1
requests==2.32.3
python-dotenv==1.0.1
cryptography==42.0.8
gunicorn==22.0.0
openpyxl==3.1.5
anthropic==0.40.0

# 连 PostgreSQL 需要这个库（部署到 Render 用 Postgres 时会用到）。装了它但本地用
# SQLite 测试完全没影响，不用单独装/卸载，所以直接放进 requirements.txt，部署时
# Build Command 不用再额外补一个 psycopg2-binary。
psycopg2-binary==2.9.9
