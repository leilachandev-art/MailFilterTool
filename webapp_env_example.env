# 这个文件是历史遗留（从 MailProcessWebsite 复制过来时带过来的旧模板），内容已经过时
# ——比如里面的 MS_CLIENT_ID/OneDrive 相关变量这个项目根本用不到，ZOHO_SCOPE 也少了
# "正文包含"搜索需要的权限。请不要照着这个文件配置环境变量。
#
# 真正准确、跟当前代码同步维护的模板是同目录下的 `.env.example`，部署到 Render 时
# 照着那个文件里的变量列表填 Environment 页面即可。
